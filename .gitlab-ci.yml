# notmyfault on GitLab: https://github.com/tashikomaaa/notmyfault/blob/main/docs/gitlab.md

include:
  - remote: https://raw.githubusercontent.com/tashikomaaa/notmyfault/v1/templates/notmyfault.gitlab-ci.yml

# One pipeline per change: merge request pipelines, and branch pipelines
# for branches without a merge request.
workflow:
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH && $CI_OPEN_MERGE_REQUESTS
      when: never
    - if: $CI_COMMIT_BRANCH

test:
  image: node:24
  # In quarantine mode, notmyfault decides whether test failures block.
  allow_failure: true
  cache:
    key:
      files: [package-lock.json]
    paths: [.npm/]
  script:
    - npm ci --cache .npm --prefer-offline
    - npx vitest run --reporter=default --reporter=junit --outputFile.junit=reports/junit.xml
  artifacts:
    when: always
    paths: [reports/]
    reports:
      junit: reports/junit.xml

notmyfault:
  extends: .notmyfault
  needs: [test]
  variables:
    NOTMYFAULT_JUNIT: reports/junit.xml
    NOTMYFAULT_KEY: test
    NOTMYFAULT_MODE: quarantine
    NOTMYFAULT_TOLERATE: flaky, broken
    NOTMYFAULT_FLAKY_ISSUES: "true"
    # NOTMYFAULT_TOKEN is a masked CI/CD variable: a project access token
    # with the api and write_repository scopes, and the Developer role.
